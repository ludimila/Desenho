<div class="row">
  <%= render partial: "courses/doctors/menu", locals: { menu: @MENU_OPTIONS[:questions] } %>

  <div class="col-xs-6 col-sm-9">
    <%= form_for(@question) do |f| %>
      <%= render partial: "shared/errors", locals: { object: @question } %>
      
      <%= f.label :command, "Comando da Questão" %>
      <%= f.text_field :command %>

      <%= f.fields_for :question_options do |option| %>
        <%= option.label :option, "teste" %>
        <%= option.text_field :option %>
      <% end %>

      <%= f.submit %>
    <% end %>

  </div>
</div>
