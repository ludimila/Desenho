<table class="table table-bordered table-striped">
  <tr class=>
    <th><%= "Curso" %></th>
    <th><%= "Carga Horária" %></th>
    <th></th>
  </tr>

  <% courses.each do |course| %>
    <tr >
      <td class="col-md-10">
        <%= link_to course.name, course %>
      </td>

      <td>
        <%= course.workload.to_s %>
      </td>

      <td>
        <% if(!course.students.exists?(current_user)) %>
          <%= link_to content_tag(:span, "", class: "glyphicon glyphicon-plus"), join_path(course), method: :post %>
        <% else %>
          <%= link_to content_tag(:i, "", class: "fa fa-sign-out"), disjoin_path(course), method: :post %>
        <% end %>
      </td>

    <% end %>
    </tr>
</table>
